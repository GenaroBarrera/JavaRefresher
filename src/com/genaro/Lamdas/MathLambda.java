package com.genaro.Lamdas;

public interface MathLambda {
    //create a functional interface that has a method with 3 parameters.
    // The first two are numbers and the last parameter is a string.
    // This method returns a value.
    // The functionality of the method is you pass the two numbers and an operand (the string) and return the result.
    // Accomplish this with lambda functions
    public int mathOps(int x, int y, String op);
}
